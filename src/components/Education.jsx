import { Typography, Box } from "@mui/material"

const Education = () => {
    return (
        <Box sx={{bgcolor: "primary.main"}}>
            <Typography variant="h3" textAlign={'center'}>Education</Typography>
        </Box>
    )
}

export default Education;